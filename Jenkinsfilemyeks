pipeline {
    agent any
    stages{
        stage('git clone'){
            steps{
                sh 'git clone https://github.com/kgovinda1995/my-eks-spring.git'
            }
        }
        stage('maven build'){
            steps{
            sh 'mvn clean install'
         }
    }
        }
         
}