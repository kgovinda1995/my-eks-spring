echo "The build number is ${env.BUILD_NUMBER}"
pipeline {
    agent any
    stages{
        stage('git clone'){
            steps{
                
                checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/kgovinda1995/my-eks-spring.git']])
            }
        }
        stage('maven build'){
            steps{
             sh 'mvn clean install'
            }
        }
        stage('docker build'){
            steps{
                script {
          def dockerTag = env.BUILD_ID
          def imageTag = "kgovinda/eks:${dockerTag}"
          
          // Build the Docker image
         sh  'docker build . -t kgovinda/eks:${imageTag} '
        }
     
      }
     }
 }
}
